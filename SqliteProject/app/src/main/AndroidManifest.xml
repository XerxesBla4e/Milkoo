<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sqliteproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Regional NOMADS"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SqliteProject">
        <activity android:name=".CreateAccount"></activity>
        <activity android:name=".HOD.HODMain" />
        <activity android:name=".HOD.HODPage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LogSign.Logn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogSign.forgotpass" />
        <activity android:name=".LogSign.Signup" />
    </application>

</manifest>